/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
      remotePatterns:[
        {
            hostname:"veracious-curlew-223.convex.cloud",
        
        }
      ]
    }
};
export default nextConfig;